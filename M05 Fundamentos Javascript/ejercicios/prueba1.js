/* const miArray = [5, 10, 2, 8, 15];
function sumarDosMayores(array) {
    // Ordena el array en orden descendente
    const arrayOrdenado = array.sort((a, b) => b - a);
    
    // Suma los dos primeros elementos del array ordenado
    const sumaDosMayores = arrayOrdenado[0] + arrayOrdenado[1];
    
    return sumaDosMayores;
}

// Ejemplo de uso

const resultado = sumarDosMayores(miArray);

console.log(`La suma de los dos números mayores es: ${resultado}`); */

/* function foo(objeto) {
    var contador = 0;
for (var clave in objeto) {
    contador ++;
}
    return contador;
}
var resultado = foo({a: 1, b: 2, c: 3});
console.log(resultado); */
/* var a =3;
var b ="3";

var resultado = a==b; */
/* let obj = {a:1, b:2, c:3} ;
delete obj.b;
console.log(obj); */

var numeros = [1,2,3,];
numeros[10]= 11;

var suma = numeros.reduce(function(a,b){
    return a + b

});





